## Installation Packages 

--------------------------------------------------

if already install some package throwing error install this package

```bash
pip install uvicorn[standard]
pip install "fastapi[standard]"
pip install pydantic

```
or
```bash

python -m pip install fastapi uvicorn sqlalchemy asyncpg
python -m uvicorn main:app --reload

```
-------------------------------------------------------------

```bash
sudo apt install alembic
```

```bash
pip install sqlalchemy alembic
pip install psycopg2-binary

```

1. asyncpg : Async PostgreSQL driver required for async support.

```bash
pip install asyncpg
```

2. Passsword will be hashing using passlib

```bash
pip install passlib
pip install bcrypt
pip install argon2_cffi
```


## Alembic migration

1. First Time  Migrations step 

```bash
alembic init alembic
```
Automatically generated by Alembic folder will be created and alembic.env.py will be created.

2. Modify env.py

```python

config.set_main_option("sqlalchemy.url", SQLALCHEMY_DATABASE_URL)

target_metadata = Base.metadata
```
3. Generating Migrations from Models

```bash

alembic revision --autogenerate -m "first migration"

```

4. Run Migrations

```bash

alembic upgrade head
```